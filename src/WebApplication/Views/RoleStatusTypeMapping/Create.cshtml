@model WebApplication.Data.RoleStatusTypeMapping

@{
    ViewData["Title"] = "Create Role Status Type Mapping ";
    var Roles = ViewBag.Roles as List<RoleViewModel>;

    var SType = ViewBag.StatusType;


}

<div class="panel mb25">
    <div class="panel-heading border">
        Create User
    </div>
    <div class="panel-body">
        <div class="row no-margin">
            <div class="col-lg-12">
                <form asp-action="Create" method="post" class="form-horizontal bordered-group" role="form">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                    <label>Role Name</label> :

                    <select name="roles" class="form-control">
                        @foreach (var group in Roles)
                        {
                            <option value="@group.Id">@group.Name</option>
                        }
                    </select>


                    <br />
                    <label>Status Type</label>

                    @foreach (var statusType in SType)
                    {

                        <div class="statustype">
                            <label for="@statusType.Name" class="cb-checkbox cb-sm">

                                <input type="checkbox" name="StatusType" data-group="@statusType.Name"
                                       value="@statusType.Name"
                                       @if (statusType != null && statusType.Name.Contains(statusType.Name)) { <text> checked</text> } />


                                <span>@statusType.Name</span>
                            </label>
                        </div>
                    }





                    <div class="form-group">
                        <div class="col-sm-offset-2 col-sm-10">
                            <input type="submit" value="Create" class="btn btn-default" />
                        </div>
                    </div>

                </form>

            </div>
        </div>
    </div>
</div>
@*@section Breadcrumbs {
        <ol class="breadcrumb">
            <li>
                <a asp-controller="Dashboard" asp-action="Index">Home</a>
            </li>
            <li>
                <a asp-controller="RoleStatusType" asp-action="Index">RoleStatus Type</a>
            </li>
            <li class="active">
                <a href="javascript:;">Create</a>
            </li>
        </ol>
    }*@
