@model IList<WebApplication.Data.MessageTemplate>

<p><a asp-action="Create">Create New</a></p>

@foreach (var item in Model)

{
   @item.Subject
   @item.Body
   @item.MailFrom
   @item.AddedOn
   @item.IsActive

}